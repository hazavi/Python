# float = decimal, or int = interger whole number
x = float(input("What's x? "))
y = float(input("What's y? "))

# round(result, amount of decimal)
z = round(x / y, 1)

#print result, f = formal string
print(f"{z}")